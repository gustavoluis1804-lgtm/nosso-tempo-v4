name: Gerar APK - Nosso Tempo

on:
  push:
    branches:
      - main

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar projeto
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Configurar Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Mostrar versões
        run: |
          echo "NODE:"
          node --version

          echo "NPM:"
          npm --version

          echo "JAVA:"
          java -version

      - name: Instalar dependências
        run: npm install

      - name: Criar projeto Android
        run: |
          if [ ! -d "android" ]; then
            npx cap add android
          fi

      - name: Sincronizar Android
        run: npx cap sync android

      - name: Aplicar configurações do Nosso Tempo
        run: node scripts/patch-android.mjs

      - name: Permitir execução do Gradle
        run: chmod +x android/gradlew

      - name: Verificar Gradle
        working-directory: android
        run: ./gradlew --version

      - name: Limpar projeto Android
        working-directory: android
        run: ./gradlew clean

      - name: Gerar APK
        working-directory: android
        run: ./gradlew assembleDebug --stacktrace

      - name: Verificar APK
        run: |
          ls -lah android/app/build/outputs/apk/debug/

      - name: Salvar APK
        uses: actions/upload-artifact@v4
        with:
          name: Nosso-Tempo-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
          retention-days: 30
